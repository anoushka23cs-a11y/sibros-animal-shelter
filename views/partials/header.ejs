<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %> | Sibro's Animal Shelter</title>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body>
  <header class="main-header">
    <h1>🐾 Sibro’s Animal Shelter 💖</h1>

    <nav class="main-nav">
      <% if (user) { %>
        <a href="/home" class="<%= active === 'home' ? 'active' : '' %>">Home</a>
        <a href="/animals" class="<%= active === 'adopt' ? 'active' : '' %>">Adopt</a>
        <a href="/donate" class="<%= active === 'donate' ? 'active' : '' %>">Donate</a>
        <a href="/volunteer" class="<%= active === 'volunteer' ? 'active' : '' %>">Volunteer</a>
        <a href="/about" class="<%= active === 'about' ? 'active' : '' %>">About</a>
        <a href="/logout">Logout</a>

      <% } else if (admin) { %>
        <a href="/admin/dashboard" class="<%= active === 'admin' ? 'active' : '' %>">Dashboard</a>
        <a href="/admin">Adoptions</a>
        <a href="/admin-volunteers">Volunteers</a>
        <a href="/admin-animals">Animals</a>
        <a href="/admin-logins">Login History</a>
        <a href="/admin/logout">Logout</a>

      <% } else { %>
        <a href="/login" class="<%= active === 'login' ? 'active' : '' %>">User Login</a>
        <a href="/admin/login" class="<%= active === 'admin' ? 'active' : '' %>">Admin Login</a>
      <% } %>
    </nav>
  </header>

  <main class="page-wrap">
