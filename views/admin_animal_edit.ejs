<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>

<header class="main-header">
  <h1>🐾 Admin - Edit Animal</h1>
</header>

<main class="page-wrap">
  <section class="page-card" style="max-width:700px;margin:0 auto;">
    <h2 class="page-title" style="text-align:center;">Edit Animal ✏️</h2>

    <form method="POST" action="/admin-animals/<%= animal.id %>/edit" class="form-grid">
      <input name="name" placeholder="Name" value="<%= animal.name %>" required />
      <input name="breed" placeholder="Breed" value="<%= animal.breed %>" required />
      <input name="health" placeholder="Health" value="<%= animal.health %>" required />
      <input name="image" placeholder="Image URL / filename" value="<%= animal.image %>" required />
      <button class="btn btn-primary" type="submit">Update Animal</button>
    </form>

    <div style="text-align:center;margin-top:14px;">
      <a href="/admin-animals" class="btn btn-soft">Back</a>
    </div>
  </section>
</main>

</body>
</html>
