<%- include('partials/header', { title: "Volunteer", active: "volunteer" }) %>

<section class="page-card">
  <h2 class="page-title">Volunteer 🐾</h2>
  <p class="page-subtitle">Your time and care can change lives.</p>

  <form action="/volunteer" method="POST" class="form-grid">
    <input type="text" name="full_name" placeholder="Your Name" required>
    <input type="email" name="email" placeholder="Your Email" required>
    <input type="text" name="phone" placeholder="Your Phone Number" required>

    <textarea name="availability" rows="4" placeholder="Your availability (days / timings)"></textarea>

    <button type="submit" class="btn btn-primary">Submit 🐾</button>
  </form>
</section>

<%- include('partials/footer') %>
